<% provide(:title, "new project") %>
<% provide(:navbar_page_title, "create new project") %>

<%= render 'projects_form', project: @project %>

<%= button_to 'back', projects_path, method: :get, class: 'btn btn-secondary' %>